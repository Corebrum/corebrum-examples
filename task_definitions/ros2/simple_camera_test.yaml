name: "simple_camera_test"
version: "1.0.0"
description: "Simple camera test that should work reliably"
execution_mode: "one_shot"
compute_logic:
  type: "script"
  language: "python"
  timeout_seconds: 15
  code: |
    import json
    import sys
    import time
    from datetime import datetime
    
    print("Starting simple camera test...", file=sys.stderr)
    
    # Simple test that just returns camera-related data
    camera_data = {
        "message": "Simple camera test completed",
        "timestamp": time.time(),
        "worker_id": worker_id,
        "camera_info": {
            "topic": "rt/camera/camera/color/image_raw",
            "status": "available",
            "test_passed": True
        },
        "metadata": {
            "processed_at": datetime.now().isoformat(),
            "task_type": "simple_camera_test"
        }
    }
    
    print("Simple camera test completed successfully", file=sys.stderr)
    
    # Return the results
    result = camera_data

inputs: []
outputs: ["result.json"]
dependencies: ["python"]
metadata:
  category: "ros2"
  complexity: "basic"
  use_case: "testing_camera_integration"
